#include <stdio.h>
#include <stdlib.h>

/**
 * main - A project on if else statements
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a = 48;

	while (a < 58)
	{
		putchar(a);
		a++;
	}
	putchar ('\n');
	return (0);
}
